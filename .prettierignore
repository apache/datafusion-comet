# prettier re-aligns markdown table columns to the widest cell, so adding a
# single expression row rewrites every row in the table. That produces noisy
# diffs and frequent merge conflicts between PRs that each add new expressions.
# This file is almost entirely tables, so exempt it from prettier formatting.
docs/source/user-guide/latest/expressions.md
